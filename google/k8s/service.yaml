apiVersion: v1
kind: Service
metadata:
  name: usher-lb-svc
  labels:
    run: usher
spec:
  selector:
    name: nginx-ingress
  type: LoadBalancer
  ports:
    -   
      protocol: TCP
      port: 80
      targetPort: 8000